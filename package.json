{
  "name": "tinksoft",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/stamp.mjs && astro build && node scripts/colophon-stats.mjs && node scripts/size-gate.mjs && node scripts/release.mjs && node scripts/checksums.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.0.0",
    "astro": "^5.0.0"
  }
}
